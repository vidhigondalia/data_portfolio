# Copy to .streamlit/secrets.toml and fill in the real values.
# secrets.toml is gitignored; this example file is not.
#
# Each entry must be a single line — a wrapped value is invalid TOML.

# Use the Supabase SESSION POOLER host (IPv4). The direct connection
# db.<ref>.supabase.co is IPv6-only and fails on IPv4 networks and on
# Streamlit Community Cloud.
SUPABASE_DB_URL = "postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# Required by the "Ask the Data" page. Create one at console.anthropic.com.
ANTHROPIC_API_KEY = "sk-ant-..."
